# delegation.md

Delegation means handing a clear proof context to a coding agent.

The handoff packet should include:

- Latest context report.
- Exact blockers.
- Allowed files and protected files.
- Commands to run.
- What receipt counts as done.

Subagents may implement fixes. ProofLoop still owns the final gate.

